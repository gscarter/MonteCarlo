name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - math
  - random
  - copy
  - networkx
  - matplotlib


    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

