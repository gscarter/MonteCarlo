name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - networkx
  - matplotlib


    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov

